2022-05-03 13:32:52 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Таблица "DATABASECHANGELOGLOCK" не найдена
Table "DATABASECHANGELOGLOCK" not found; SQL statement:
SELECT COUNT(*) FROM DATABASECHANGELOGLOCK [42102-200]
2022-05-03 13:35:58 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
);"; expected "identifier"; SQL statement:


CREATE TABLE ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id)
); [42001-200]
2022-05-03 13:37:47 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Таблица "USERS" уже существует
Table "USERS" already exists; SQL statement:
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR NOT NULL,
  email VARCHAR NOT NULL UNIQUE,
  phone VARCHAR NOT NULL UNIQUE
); [42101-200]
2022-05-03 13:38:36 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Таблица "USERS" уже существует
Table "USERS" already exists; SQL statement:
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR NOT NULL,
  email VARCHAR NOT NULL UNIQUE,
  phone VARCHAR NOT NULL UNIQUE
); [42101-200]
2022-05-03 18:53:20 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Таблица "USERS" уже существует
Table "USERS" already exists; SQL statement:
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  username VARCHAR NOT NULL,
  email VARCHAR NOT NULL UNIQUE,
  phone VARCHAR NOT NULL UNIQUE
); [42101-200]
2022-05-03 18:54:04 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id)
); [42001-200]
2022-05-03 18:58:01 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-03 19:05:00 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-03 19:07:12 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-03 19:07:47 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-03 20:19:23 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-17 19:48:09 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-17 19:51:07 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS SESSIONS (
  ID SERIAL PRIMARY KEY,
  'name'[*] TEXT
);

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    'row' INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    UNIQUE (SESSION_ID, `ROW`, CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS SESSIONS (
  ID SERIAL PRIMARY KEY,
  'name'[*] TEXT
);

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    'row' INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    UNIQUE (SESSION_ID, `ROW`, CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists sessions (
  id SERIAL PRIMARY KEY,
  'name' text
);

CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    'row' INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    UNIQUE (session_id, `row`, cell)
); [42001-200]
2022-05-17 19:53:27 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-17 19:56:30 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-17 19:57:55 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "

CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);"; expected "identifier"; SQL statement:


CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
); [42001-200]
2022-05-17 20:03:15 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);

CREATE TABLE IF NOT EXISTS USERS (
  ID SERIAL PRIMARY KEY,
  USERNAME VARCHAR NOT NULL,
  EMAIL VARCHAR UNIQUE,
  PHONE VARCHAR UNIQUE
);

CREATE TABLE IF NOT EXISTS SESSIONS (
  ID SERIAL PRIMARY KEY,
  NAME TEXT
);"; ожидалось "identifier"
Syntax error in SQL statement "CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID),
    CONSTRAINT SESSION_UNIQUE UNIQUE (SESSION_ID),
    CONSTRAINT ROW_UNIQUE UNIQUE (ROW),
    CONSTRAINT CELL_UNIQUE UNIQUE (CELL)
);

CREATE TABLE IF NOT EXISTS USERS (
  ID SERIAL PRIMARY KEY,
  USERNAME VARCHAR NOT NULL,
  EMAIL VARCHAR UNIQUE,
  PHONE VARCHAR UNIQUE
);

CREATE TABLE IF NOT EXISTS SESSIONS (
  ID SERIAL PRIMARY KEY,
  NAME TEXT
);"; expected "identifier"; SQL statement:
CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id),
    CONSTRAINT session_unique UNIQUE (session_id),
    CONSTRAINT row_unique UNIQUE (row),
    CONSTRAINT cell_unique UNIQUE (cell)
);

CREATE TABLE if not exists users (
  id SERIAL PRIMARY KEY,
  username VARCHAR NOT NULL,
  email VARCHAR UNIQUE,
  phone VARCHAR UNIQUE
);

CREATE TABLE if not exists sessions (
  id SERIAL PRIMARY KEY,
  name text
); [42001-200]
2022-05-18 11:18:29 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "
CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
    CONSTRAINT UNIQUE_TICKET UNIQUE (SESSION_ID, ROW, CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "
CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
    CONSTRAINT UNIQUE_TICKET UNIQUE (SESSION_ID, ROW, CELL)
);"; expected "identifier"; SQL statement:

CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id)
    CONSTRAINT unique_ticket UNIQUE (session_id, row, cell)
); [42001-200]
2022-05-18 11:20:04 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "
CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INTEGER,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
    CONSTRAINT UNIQUE_TICKET UNIQUE (SESSION_ID, ROW, CELL)
);"; ожидалось "identifier"
Syntax error in SQL statement "
CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROW[*] INTEGER,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
    CONSTRAINT UNIQUE_TICKET UNIQUE (SESSION_ID, ROW, CELL)
);"; expected "identifier"; SQL statement:

CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    row integer,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id)
    CONSTRAINT unique_ticket UNIQUE (session_id, row, cell)
); [42001-200]
2022-05-18 11:21:23 jdbc[3]: exception
org.h2.jdbc.JdbcSQLSyntaxErrorException: Синтаксическая ошибка в выражении SQL "
CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROWS INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
    CONSTRAINT[*] UNIQUE_TICKET UNIQUE (SESSION_ID, ROW, CELL)
);"; ожидалось "INDEX, ON, NOT, DEFERRABLE, ,, )"
Syntax error in SQL statement "
CREATE TABLE IF NOT EXISTS TICKET (
    ID SERIAL PRIMARY KEY,
    SESSION_ID INT NOT NULL REFERENCES SESSIONS(ID),
    ROWS INT NOT NULL,
    CELL INT NOT NULL,
    USER_ID INT NOT NULL REFERENCES USERS(ID)
    CONSTRAINT[*] UNIQUE_TICKET UNIQUE (SESSION_ID, ROW, CELL)
);"; expected "INDEX, ON, NOT, DEFERRABLE, ,, )"; SQL statement:

CREATE TABLE if not exists ticket (
    id SERIAL PRIMARY KEY,
    session_id INT NOT NULL REFERENCES sessions(id),
    rows INT NOT NULL,
    cell INT NOT NULL,
    user_id INT NOT NULL REFERENCES users(id)
    CONSTRAINT unique_ticket UNIQUE (session_id, row, cell)
); [42001-200]
